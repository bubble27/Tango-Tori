<?xml version="1.0" encoding="utf-8"?>
<!-- Transparent placeholder for windowSplashScreenAnimatedIcon so the
     adaptive-icon circle slot is invisible, leaving only the background. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
</shape>
